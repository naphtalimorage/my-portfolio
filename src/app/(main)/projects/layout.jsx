export const metadata = {
    title: "projects",
    description: "Projects page"
}

export default function RootLayout({children}) {
    return (
        <>
            {children}
        </>
    );
}